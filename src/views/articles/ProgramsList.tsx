import React from "react";
import { IArticleData } from "../../models/IArticlesData";

const ProgramsList = ({
  name,
  title,
  date,
  image,
  imgAlt,
  icon,
  description,
  featured,
  highLight,
  content,
}: IArticleData) => {
  return <div></div>;
};

export default ProgramsList;
